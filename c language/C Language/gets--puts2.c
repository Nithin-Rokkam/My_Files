//wap to ask ue=ser to print name  city state

#include<stdio.h>
#include<conio.h>
int main(){
	char ch[100];
	printf("Enter the details:");
	gets(ch);
	puts("Your details are:");
	puts(ch);
	return 0;
}
