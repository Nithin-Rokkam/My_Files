#include<iostream>
using namespace std;

class Node{
	public:
		int data;
		Node* next;
		
	Node(int data){
		this->data=data;
		this->next=NULL;
	}
};

void insertAtStart(Node* &head.int data){
	Node* start=new Node(data);
	start->next=head;
	start=head;
}


void insertAtLast(Node* &tail,int data){
	Node* end=new Node(data);
	tail.next=end;
	tail=end;
}

void insertAtPos(Node* &tail,Node* &head,int pos,int data){
	if(pos==1){
		insertAtStart(data);
		return;
	}
	
	int count=1;
	Node* temp=head;
	while(count<pos-1){
		temp=temp->next;
		count++
	}
	Node* insertNode=new Node(data);
	insertNode->next=temp->next;
	temp->data=insertNode;
}

void print(Node* &head){
	Node* temp=head;
	while(temp!=NULL){
		cout<<temp->data<<" ";
		temp=temp->next;		
	}
}

void print(Node* &head){
	
}

int main(){
	Node* head=NULL;
	Node* tail=NULL;
	int n;
	while(true){
		cin>>n;
		if(n==-1){
			break;
		}
		insertAtPos(head,tail,NULL,NULL);
	}
	print(head);
	return 0;
}
